[project]
name = "papernews"
version = "0.1.0"
description = "A lightweight HN-like website for research discussion"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.5",
    "django-environ>=0.13.0",
    "gunicorn>=26.0.0",
    "nh3>=0.3.5",
]
